diff --git a/src/pages/GiftCards.tsx b/src/pages/GiftCards.tsx
index 95bb314..e8b1f93 100644
--- a/src/pages/GiftCards.tsx
@@ -38,9 +38,17 @@ export default function GiftCards() {
     message: z.string().max(1000).nullable().optional()
   });
 
+  const handleCreate = async () => {
+    setBusy(true);
+    let validatedRecipient;
+    try {
+      validatedRecipient = giftCardSchema.parse(recipient);
+    } catch (e: any) {
+      setBusy(false);
       toast.error(e.errors[0].message);
       return;
     }
+
     const { data, error } = await supabase.rpc('admin_create_gift_card', {
       _amount: amount,
       _recipient_name: validatedRecipient.name,